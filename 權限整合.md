# STANDO 四工具 帳號與權限整合

> 對象:ELSA 2.0、CHEF、Worker、OREO 四個工具 + STANDO Portal 門戶
> 後端共用 Supabase 專案 `jpzgueafidiwmdosivxa`
> 本文件為整合現況盤點 + 整合決策 + 實作計劃。先確認本文,再動程式。

---

## 1. 共同骨架(四工具已統一)

所有工具共用同一個 Supabase 專案、同一張帳號表 `public.profiles`(一對一對應 `auth.users`)。

`public.profiles` 欄位:

| 欄位 | 說明 |
|------|------|
| `id` (uuid) | = `auth.users.id`,主鍵 |
| `display_name` | 暱稱(給管理者辨識) |
| `email` | 信箱 |
| `phone` | 電話 |
| `department` | 部門 |
| `is_active` (bool) | 帳號是否啟用;`false` 時全工具一次擋掉 |
| `tool_roles` (jsonb) | 各工具角色,例:`{"elsa":"owner","chef":"admin","worker":"worker","oreo":"office"}` |
| `created_at` / `updated_at` | 時間戳 |

規則:**每個工具只讀自己的 key**;沒有該 key = 該工具無權限。

---

## 2. 各工具角色等級對照

### 統一詞彙(ELSA / CHEF / Worker 共用)
`owner` 擁有者 > `admin` 管理員 > `worker` 作業員 > `user` 一般 > `guest` 訪客
> 詞彙統一,但**實際權限由各工具自己的 RLS 定義**,同一個詞在不同工具不代表同樣權限。

| 工具 | 讀取 | 能進入的角色與權限 |
|------|------|-------------------|
| **ELSA** | `tool_roles.elsa` | `owner` 全讀寫 + 使用者管理 / `admin` 改資料庫(不含使用者) / `user` 唯讀。worker·guest·無 = 無權限 |
| **CHEF** | `tool_roles.chef` | 只有 `owner`、`admin` 能進(RLS 鎖 owner/admin),其餘一律「本工具無權限」 |
| **Worker** | `tool_roles.worker` | `owner` 最高(資料同 admin + 管帳號) / `admin`(生管)全存取案件·派工·回報 / `worker`(工務)可讀全部任務、只能改自己任務狀態與回報。user·guest·無 = 無權限 |
| **OREO** | `tool_roles.oreo` | **自成一套**(流程角色,非階級):`admin` 管理員 / `office` 辦公室(開案·下單) / `warehouse` 倉管(收貨) / `factory` 工廠(收貨)。無 = 導回 |

### OREO 為什麼不一樣
OREO 的角色是「**誰負責哪段流程**」(辦公室開單、倉/廠收貨),不是高低階級,套 owner/admin 反而失真。**整合決策:OREO 維持自己這套,不硬統一。**

---

## 3. 後端輔助函式(RLS 用,皆 SECURITY DEFINER)

| 函式 | 作用 |
|------|------|
| `public.current_tool_role(tool)` | 回 `tool_roles->>tool`;但 `is_active=false` 時回 `NULL`(停用帳號全工具擋掉) |
| `public.is_tool_owner(tool)` | 是否為「指定工具」的 owner(ELSA 階段建立) |
| `public.is_owner()` | 是否為「**任一工具**」的 owner —— 帳號管理現在用這個 |
| `public.list_users()` (RPC) | 列出所有使用者給管理介面用,gate = `is_owner()` |

> **重點更正**:帳號管理權限早期綁 `is_tool_owner('elsa')`(只有 ELSA owner),Worker 階段已升級為 `is_owner()`(**任一工具的 owner 都能管帳號**)。`profiles` 的 select/write owner policy 現在都走 `is_owner()`。

---

## 4. 帳號生命週期(現況)

1. **註冊**:新人填 email + 密碼 + 暱稱 → `auth.users` 新增 → trigger 自動建 `profiles`,`tool_roles = {}`(無任何權限)
2. **確認信** → 可登入,但每個工具都顯示「本工具無權限,請聯絡管理者小黃」
3. **開通**:owner 進管理介面 → 設定該人各工具的角色
4. **登入**:在各工具**各自網域**登入(見下方限制)
5. **改密碼**:登入後於帳號設定改;或走忘記密碼流程
6. **停用**:`is_active = false` → 一次擋掉全部工具

### ⚠️ 跨網域限制(影響整合範圍)
四工具是**不同網域**(elsa20 / chef- / oreo- / worker-.vercel.app),Supabase 登入狀態存在各網域的瀏覽器,**無法在 Portal 登入就讓你在各工具也登入**。
→ 所以**「登入」必須留在各工具自己身上**;只有跟登入無關的流程(申請帳號、忘記密碼、改密碼、權限管理)能集中到 Portal。

---

## 5. 不一致點盤點

| # | 不一致 | 現況 | 處理 |
|---|--------|------|------|
| 1 | OREO 角色詞彙 | office/warehouse/factory/admin,與其他三個不同 | **不硬統一**,接受 OREO 自成一套 |
| 2 | 使用者管理介面位置 | 目前在 ELSA `/admin`,且只能編輯 elsa/chef/worker(**無 oreo**) | **移到 Portal**,做集中管理表單(含 OREO) |
| 3 | 帳號管理 gate | 已從 `is_tool_owner('elsa')` 升級為 `is_owner()`(任一工具 owner) | 維持,Portal 管理表單沿用 |
| 4 | `is_active` 前端檢查 | CHEF/Worker/OREO 前端都檢查;ELSA 前端沒檢查(但後端 RLS 會擋) | 體驗差異,可改可不改,優先度低 |

---

## 6. 整合決策(依討論結果)

1. **角色詞彙**:ELSA/CHEF/Worker 維持 owner/admin/worker/user/guest;OREO 維持自己那套。
2. **帳號中樞集中在 Portal**:
   - 申請帳號(註冊)
   - 忘記密碼(已存在 `reset.html`)
   - 帳號設定(改密碼)
   - **使用者/權限管理表單**(能設四工具角色,含 OREO)→ 從 ELSA 移過來
3. **登入**:維持在各工具(網域限制),各工具登入框放「申請帳號 → Portal」「忘記密碼 → Portal」連結。
4. ELSA `/admin` 的使用者分頁:Portal 管理表單上線後可移除(避免兩處維護)。

---

## 7. 實作計劃(待你確認後動手)

### A. Portal 申請帳號頁 `register.html`
- 欄位:暱稱、Email、密碼;`supabase.auth.signUp`,帶 `display_name`
- 成功後提示「已寄確認信,確認後等管理員開通權限」
- 風格沿用現有 `reset.html`

### B. Portal 帳號設定頁 `account.html`
- 需先登入(`getUser`,未登入導回);`updateUser({ password })` 改密碼
- 風格沿用 `reset.html`
- ⚠️ 受跨網域限制:此頁的「已登入」只認 Portal 網域的 session。需確認使用情境(多半從各工具帶過來，或當作獨立改密碼頁需先在此登入一次)。實作前再對齊。

### C. Portal 使用者管理表單(取代 ELSA `/admin` 使用者分頁)
- 用 `list_users()` RPC 列出所有人(gate 已是 `is_owner()`)
- 每人可編輯:暱稱、部門、`is_active`、四工具角色
  - ELSA / CHEF / Worker:下拉 owner/admin/worker/user/guest/(無)
  - OREO:下拉 admin/office/warehouse/factory/(無)
- 寫回 `profiles`(保留未顯示的 key,只覆寫這四個)
- ⚠️ 同樣需先在 Portal 登入(且為任一工具 owner)才能用

### D. ELSA 後續
- Portal 管理表單上線、驗證無誤後,移除 ELSA `/admin` 的「使用者」分頁(本輪不做,等 Portal 版穩定)

---

## 8. 待確認問題

- **B/C 兩頁的登入**:Portal 自己要不要做一個登入框(讓 owner 能在 Portal 登入後管帳號、讓使用者能在 Portal 登入後改密碼)?還是改密碼一律走「忘記密碼」流程、不做 account 頁?
  → 這會影響 B、C 是否成立,動手前需先定。
