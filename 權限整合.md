# STANDO 帳號與權限總覽

> 所有工具共用 Supabase 專案 `jpzgueafidiwmdosivxa`。**帳號相關的事一律以 Portal 為準。**
> 最後更新：2026-09-13

---

## 1. 要做什麼 → 去哪裡

| 要做的事 | 去哪 |
|---|---|
| 新人申請帳號 | Portal `register.html` |
| **核准、派權限（含工廠關卡）** | Portal 首頁登入 → 右上「使用者管理」`admin.html`，待開通的人排最上面 |
| 停用帳號 | 同上，編輯 → 取消「帳號啟用中」 |
| 忘記密碼 | Portal `reset.html` |
| 改密碼 | Portal `account.html` |
| 登入 | 各工具自己的網站（網域不同，無法共用登入狀態） |
| 查／改後端規則 | Portal `supabase/`（見 §4） |

---

## 2. 開通流程

1. 新人在 `register.html` 填暱稱、Email、密碼 → 收確認信點連結
2. 資料庫自動建一筆 `profiles`，沒有任何權限（= 待開通）
3. 管理者（任一工具的 owner）進 `admin.html` → 待開通區 → 編輯 → 設各工具角色 → 儲存
4. 新人到各工具網站登入

---

## 3. 各工具

| 工具 | 網址 | 權限存哪 | 角色 | 登入程式在哪 |
|---|---|---|---|---|
| ELSA 2.0 | elsa20.vercel.app | `tool_roles.elsa` | owner 全讀寫＋管帳號 / admin 改資料庫 / user 唯讀 | `ELSA2.0/src/lib/auth.ts` |
| ELSA 3.0 | elsa3-0.vercel.app | `tool_roles.elsa`（與 2.0 同一個） | 同 2.0 | `ELSA3.0/src/lib/auth.ts`、`src/components/AuthGate.tsx` |
| CHEF | chef-system-ten.vercel.app | `tool_roles.chef` | 只有 owner、admin 能進 | `CHEF-system/src/App.jsx` |
| Worker | worker-system-dusky.vercel.app | `tool_roles.worker` | owner 管帳號 / admin 生管 / worker 工務（只能改自己的任務） | `worker_system/src/stores/auth.ts` |
| OREO | oreo-ten.vercel.app | `tool_roles.oreo` | admin / office 開案下單 / warehouse、factory 收貨 | `oreo/web/lib/auth.ts` |
| Questboard | questboard-psi.vercel.app | `tool_roles.questboard` | owner 核准順序、看承諾日 / sales 看承諾日 / user 設計師 | `questboard/app/page.tsx` |
| 工廠關卡 | factoryboard-bice.vercel.app | **`factoryboard.app_user`**（不在 tool_roles） | owner / office 辦公室 / manager 廠長 / station（綁一關） | `factoryboard/app/login/page.tsx`、`app/layout.tsx` |

- 角色詞彙相同不代表權限相同，實際權限由各工具自己的資料庫規則決定。
- OREO 的角色是「誰負責哪段流程」，刻意不跟其他工具統一。
- 各工具最高權限一律叫 `owner`；**`boss` 是老闆本人的稱呼，不當角色名**（工廠關卡的廠長角色 2026-09-13 從 boss 改為 manager）。

### ⚠️ 工廠關卡要注意
- **名單獨立**：Portal 停用帳號擋不到工廠關卡，要擋請把工廠關卡設成「（無權限）」。
- 工廠關卡自己的 `/users` 頁仍可用（能直接建帳號、重設密碼），但日常開通請統一用 Portal。
- 在工廠關卡留過操作紀錄的人，資料庫可能不讓移除角色，改成其他角色即可。

---

## 4. 後端規則（放在 Supabase，定義收在 Portal `supabase/`）

| 檔案 | 內容 |
|---|---|
| `supabase/auth_snapshot.sql` | 現行定義快照：profiles 表、註冊觸發器、權限函式、存取規則。**參考用，不要整份重跑** |
| `supabase/migrations/` | 之後帳號、權限相關的資料庫變更都放這裡（含跨工具的資安收緊 `20260913000002`） |

**讀取規則一律要求「有該工具角色」（不可違反）**：絕不寫 `using (true)`、也不能只看是否登入，照片空間也絕不開放未登入的人列清單。原因是 Portal 開放申請帳號，陌生人註冊並驗證信箱後就算是「已登入」。2026-09-13 已把工廠關卡、OREO 廠商、Worker／工廠照片空間全部改成這樣。

| 函式 | 作用 |
|---|---|
| `current_tool_role(tool)` | 登入者在某工具的角色；帳號停用時回 NULL |
| `is_tool_owner(tool)` | 是否為指定工具的 owner |
| `is_owner()` | 是否為任一工具的 owner（帳號管理的門檻） |
| `list_users()` | 管理頁列出所有人 |
| `handle_new_user()` | 註冊時自動建 profiles |

早期定義散在 `ELSA2.0/supabase/migrations/0014–0016`、worker 等專案，有些只存在資料庫裡、本機沒有檔案。**一律以快照為準**，舊檔保留不刪。

---

## 5. 待處理


- ELSA 2.0 `/admin` 還留著舊的使用者分頁（`src/pages/Admin.tsx`），只管得到部分工具，之後建議移除
- ELSA 前端沒檢查 `is_active`（後端會擋，只是畫面體驗不同）
- pricemana、auth-reset 也連到這個 Supabase，但不走 `tool_roles`，尚未整合
